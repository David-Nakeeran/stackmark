<h1>Bookmarks</h1>
<div>
  <% unless @bookmarks.any? %>
    <p>You currently don't have any bookmarks</p>
  <% else %>
    <% @bookmarks.each do |bookmark|%>
      <div>
        <p>Title: <%= bookmark.title%></p>
        <%= link_to "View Details", bookmarks_path(bookmark)%>
      </div>
    <%end%>
  <%end%>
</div>
